import 'package:flutter/material.dart';

class Ahiahi extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Next"),
      ),
      body: Container(
        child: Center(
          child: Column(
            children: <Widget>[
              Text("Fuk You"),

            ],
          ),
        ),
      ),
    );
  }
}
